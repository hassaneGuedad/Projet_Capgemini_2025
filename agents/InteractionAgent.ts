export async function InteractionAgent(question: string): Promise<string> {
    // Appelle Claude pour demander une clarification à l'utilisateur
    // (À intégrer côté UI pour interaction)
    return question;
  }